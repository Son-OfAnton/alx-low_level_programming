#include <stdio.h>
#include "cfunctions.h"
/**
 * sub - the function that subtracts b from a.
 *@a: parameter of the funtion.
 *@b: parameter of the function.
 * Return: returns difference.
 */
int sub(int a, int b)
{
	return (a - b);
}
