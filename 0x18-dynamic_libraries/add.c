#include <stdio.h>
#include "cfunctions.h"
/**
 * add - adds two integers
 *@a: parameter of the funciton.
 *@b: parameter of the function.
 * Return: returns the sum.
 */
int add(int a, int b)
{
	return (a + b);
}
